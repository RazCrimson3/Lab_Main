#include <stdio.h>
#include <stdlib.h>

int main()
{
	short i;
	int n,m,sum=0;
	printf("\nEnter the values of n and m : ");
	scanf("%d%d",&n,&m);
	if(n<m)
	{
		printf("The sum of values between n amd m is : ");
		for(;i<=m;i++)
			sum+=;
	printf("%d",sum);
	}
	else
	{
		printf("\nERROR!!");
		exit(7);
	}
	printf("\n");
	return 0;
}
